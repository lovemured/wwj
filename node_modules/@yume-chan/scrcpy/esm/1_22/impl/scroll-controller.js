import { extend, s32 } from "@yume-chan/struct";
import { PrevImpl } from "./prev.js";
export const InjectScrollControlMessage = extend(PrevImpl.InjectScrollControlMessage, { buttons: s32 });
export class ScrollController extends PrevImpl.ScrollController {
    serializeScrollMessage(message) {
        const processed = this.processMessage(message);
        if (!processed) {
            return undefined;
        }
        return InjectScrollControlMessage.serialize(processed);
    }
}
export function createScrollController() {
    return new ScrollController();
}
//# sourceMappingURL=scroll-controller.js.map