const EncoderRegex = /^\s+--(video|audio)-codec=(\S+)\s+--\1-encoder='([^']+)'$/;
export function parseEncoder(line) {
    const match = line.match(EncoderRegex);
    return match
        ? {
            type: match[1],
            name: match[3],
            codec: match[2],
        }
        : undefined;
}
//# sourceMappingURL=parse-encoder.js.map