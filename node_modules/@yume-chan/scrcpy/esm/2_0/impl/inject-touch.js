import { struct, u16, u32, u64, u8 } from "@yume-chan/struct";
import { ScrcpyControlMessageType } from "../../base/control-message-type.js";
import { PrevImpl } from "./prev.js";
export const InjectTouchControlMessage = /* #__PURE__ */ (() => struct({
    type: u8(ScrcpyControlMessageType.InjectTouch),
    action: u8(),
    pointerId: u64,
    pointerX: u32,
    pointerY: u32,
    videoWidth: u16,
    videoHeight: u16,
    pressure: PrevImpl.UnsignedFloat,
    actionButton: u32,
    buttons: u32,
}, { littleEndian: false }))();
export function serializeInjectTouchControlMessage(message) {
    return InjectTouchControlMessage.serialize(message);
}
//# sourceMappingURL=inject-touch.js.map