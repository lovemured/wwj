# 接口测试用例模板（可复制粘贴）

> 用法：每条用例复制一份本模板填写；如果你们用 XMind/Excel/TestRail/Jira，也可将字段映射过去，核心是保证“可执行、可断言、可追溯”。

---

## 基本信息
- 用例编号：
- 用例标题：
- 优先级：P0 / P1 / P2
- 用例类型：FUNC / VALID / SEC / PERF / STAB
- 所属服务/模块：
- 关联需求/故事/缺陷（链接或编号）：
- 版本/迭代：
- 作者/日期：

---

## 接口信息
- Method：
- Path：
- Content-Type：`application/json` / `multipart/form-data` / 其他
- 鉴权方式：Bearer Token / Cookie / 签名 / 无
- 是否幂等：是 / 否（如是，幂等键字段：）

---

## 用例目标（写清楚要验证的规则）
- 目标/风险点：
  - 示例：验证 `pageSize` 超过上限时返回统一参数错误码
  - 示例：验证用户 A 不可读取用户 B 的资源（水平越权）

---

## 前置条件（必须可准备）
- 账号与权限：
- 数据准备：
  - 资源 ID/订单号/商品 SKU：
  - DB/后台造数方式：
- 依赖服务：
  - 下游是否需要 Mock：
  - 开关/灰度：

---

## 请求（尽量可直接执行）

### Headers
```http
Authorization: Bearer <token>
X-Request-Id: <uuid>
Idempotency-Key: <uuid>  # 如需要
```

### Query / Path Params
```text
page=1
pageSize=20
```

### Body（JSON 示例）
```json
{
  "fieldA": "value",
  "fieldB": 1
}
```

---

## 执行步骤
1. （可选）准备数据：……
2. 发送请求：……
3. 校验响应：……
4. （可选）校验副作用：DB/缓存/消息/日志

---

## 预期结果（必须可断言）

### 响应断言
- HTTP 状态码：
- 业务码/错误码：
- 错误信息（必要时精确到字段）：
- 响应结构断言：
  - 字段存在性：
  - 字段类型：
  - 枚举/范围：
  - 可空性：
- 关键业务断言：
  - 示例：`data.status` 必须为 `ACTIVE`

### 副作用断言（如适用）
- DB：
  - 表：
  - 关键字段与期望值：
- 缓存：
  - Key：
  - 期望：写入/失效/不变
- 消息/任务：
  - Topic/事件名：
  - 期望：发送 1 条 / 不发送
- 审计/操作日志：
  - 期望：记录包含 userId、resourceId、action

---

## 清理与回滚（可选但推荐）
- 删除创建的资源：
- 回滚 DB：
- 恢复开关/Mock：

---

## 自动化信息（可选）
- 是否可自动化：是 / 否（原因：依赖人工/验证码/外部系统等）
- 自动化实现建议：
  - Postman 集合：
  - pytest 脚本路径：
  - 需要的测试夹具/造数脚本：
- 注意事项（易波动点、断言口径、数据隔离策略）：

